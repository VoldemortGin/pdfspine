# Rust
/target/
**/target/
Cargo.lock.bak
*.rs.bk
*.pdb

# Python / PyO3 / maturin
__pycache__/
*.py[cod]
*.so
*.pyd
*.dylib
*.dSYM/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
wheelhouse/

# Coverage / profiling
*.profraw
/coverage/
tarpaulin-report.html

# Fuzzing
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/

# Conformance / validation harness
# Local-only diff-oracle venv (PyMuPDF is AGPL — never bundled/committed/shipped).
.venv-oracle/
# Local-only benchmark venv (pypdf MIT / pypdfium2 BSD — bench-only, never shipped).
.venv-bench/
# Tier-2 fetch-only corpus of unknown copyright — robustness only, NEVER committed.
conformance/corpus-cache/
# Harness machine-readable output (REPORT.md / BENCH.md are committed; raw JSON is not).
conformance/results.json
conformance/fetch-summary.json
conformance/bench-results.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
