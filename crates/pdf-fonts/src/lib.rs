#![forbid(unsafe_code)]
//! `pdf-fonts` — font parsing for *mapping* (encodings, `Differences`,
//! `ToUnicode`, CJK CMaps, widths/metrics, glyph -> Unicode). No rasterization.
//!
//! Placeholder for M0. Implemented in M2 per PRD §7.
