# Color

`Colorspace` describes a pixel colorspace. The DeviceGray / DeviceRGB /
DeviceCMYK singletons are re-exported as
[`csGRAY` / `csRGB` / `csCMYK`](constants.md#colorspace-singletons-font-tables).

::: pdfspine.Colorspace
